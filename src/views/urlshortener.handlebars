<a href="http://localhost:{{port}}/homePage"> Home page </a>

<div>
  {{#each currentUrls}}
    <p> {{this}} </p>
  {{/each}}
</div>
<p>
  {{problem}}
</p>

<p>
  <form action="/shortUrls" method="post">
    <label for="newUrl">New Url </label>
    <input
      type="text"
      id="newUrl"
      name="newUrl"
      placeholder="localhost{{porst}}/..."
    />
    <input type="submit" value="short url" />
  </form>
</p>